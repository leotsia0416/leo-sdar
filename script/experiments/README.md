These scripts are manual experiment drivers.

They are usually submitted directly with `sbatch` or run manually to launch
specific sweeps, comparisons, or one-off evaluations. They are not core helper
utilities consumed by the main training and evaluation entrypoints.
