These scripts are for manual analysis, plotting, and single-case inspection.

They are intentionally separated from `script/helpers/`, which is reserved for
scripts used directly by the main training and evaluation flows.

HTML timeline renderers default to writing outputs under `artifacts/html/`.
